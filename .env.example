# Tauri updater signing for local `npm run build:tauri`.
# CI uses the GitHub secret TAURI_SIGNING_PRIVATE_KEY.
# If src-tauri/.updater-key.pem exists, build:tauri loads it automatically.
# TAURI_SIGNING_PRIVATE_KEY=
