Desktop installers for Windows, Linux, and macOS.

## Fixes
- Update check no longer reports "latest version" when the check actually failed
- Settings now show the installed app version
- Fallback updater endpoint via raw.githubusercontent.com
